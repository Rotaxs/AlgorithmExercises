#include <iostream>
using namespace std;

const int N = 3 * 1e4 + 10;
int n;
int arr[N];

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    for (int i = 1; i <= n; i++)
        cin >> arr[i];
    cin >> n;

    return 0;
}