#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

const int N = (1 << 10) + 1;
int ans[N][N << 1];

void solve()
{
    int n; cin >> n;

    function<void(int, int, int)> dfs = [&](int x, int y, int s)
    {
        
    };
}

int main()
{
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    // int t;
    // cin >> t;
    // while (t--) solve();

    solve();
    
    return 0;
}